# ROS 2 ビジュアルレーシングシステム システム概要＆機能仕様書

本ドキュメントは、`/home/user25/raphael_ws/src/simulation`（3Dシミュレータ環境）および `/home/user25/raphael_ws/src/raphael_enterprise/raphael_enterprise/course_lap_node.py`（ビジュアルレーシングAIノード）に現在導入されている全機能と処理内容をまとめた仕様書である。

---

## 1. 全体構造と処理フロー

本システムは **「3D環境物理・ビジョンシミュレータ（simulation）」** と **「自律走行ビジュアルサーボAI（course_lap_node）」** が ROS 2 トピックを介してリアルタイムに閉ループ（フィードバック）制御を行う構成となっている。

```text
  ┌────────────────────────────────────────────────────────────────────────┐
  │                        3D シミュレータ (simulation)                    │
  │                                                                        │
  │  [Panda3D 3D空間]                                                       │
  │     ├─ 2D/3Dコース描画 & 車両 (DifferentialDriveRobot) 運動方程式       │
  │     ├─ ウィンドウ1: 一人称視点 (FPV) カメラ画面をレンダリング           │
  │     └─ ウィンドウ2: 上空150m固定Top View全景マップを表示 (独立カメラ)   │
  └───────────────────────────────────┬────────────────────────────────────┘
                                      │  Publish: /camera/image_raw
                                      │  (カメラ画像 640x480)
                                      ▼
  ┌────────────────────────────────────────────────────────────────────────┐
  │                    ビジュアルレーシング AI (course_lap_node)            │
  │                                                                        │
  │  1. 【安全装置】 画面下部(85~100%)で緑色(芝生)が80%超で緊急停止        │
  │  2. 【高速カーブ検知】 Y=0.55の路面重心ズレ(15%超)で直線/カーブ状態遷移 │
  │  3. 【連続トラッキング】 足元(0.90)→近景(0.80)→中間(0.70)→遠方(0.60)   │
  │     の順に探索領域(±150px)を限定し、最大路面ブロックの中心を連鎖抽出  │
  │  4. 【4点Polyfit軌道推定】 4点からnp.polyfitで2次代数曲線(X=aY²+bY+c)算出│
  │  5. 【PID制御】 追従Y目標(0.625)でのtarget_xと画面中央の差から角速度w算出│
  └───────────────────────────────────┬────────────────────────────────────┘
                                      │  Publish: /cmd_vel
                                      │  (線速度 v [m/s], 角速度 w [rad/s])
                                      ▼
  ┌────────────────────────────────────────────────────────────────────────┐
  │  [simulation/main.py]                                                  │
  │     └─ /cmd_vel を受信して車両の物理状態(x, y, theta)をリアルタイム更新  │
  └────────────────────────────────────────────────────────────────────────┘
```

---

## 2. モジュール別導入機能・処理詳細

### 2-1. `src/simulation` (3D環境・物理シミュレータパッケージ)

Panda3D 3Dグラフィックスエンジンと ROS 2 (`rclpy`) を相乗り実行し、物理演算と高精度カメラ画像のパブリッシュを行う。

#### 1. `main.py` (メインエントリーポイント)
* **ROS 2 ＆ Panda3D 統合ループ**: Panda3Dのタスクマネージャ (`taskMgr`) 内で毎フレーム `rclpy.spin_once()` を呼び出し、`/cmd_vel` の受診と3D描画・画像配信のメインループを同時駆動。
* **コース・車両・ノード初期化**: `CourseGenerator` から2D/3Dコース形状と16箇所のノード位置を生成し、ロボットおよびビジュアルライザへ引き渡す。
* **ラップタイム・通過管理**: 車両位置と16箇所のノード座標の距離をチェックし、周回状態（ラップ完了）を監視。

#### 2. `visualizer.py` (マルチウィンドウ 3D描画 & カメラ)
* **3Dオブジェクト描画**:
  - **茶色の大地＆緑の芝生**: コース周囲4mに緑の芝生ポリゴンを生成（Z=-0.01）。
  - **黒色路面**: コース幾何点から黒色アスファルトポリゴンを生成（Z=0.0）。
  - **白線**: コース外側・内側・中心線をラインセグメント描画（Z=0.01）。
  - **ロボットモデル**: 青色直方体（Scale: 0.8×1.0×0.4）を自機として配置（Z=0.1）。
  - **ノードマーカー**: 16箇所のコントロールポイントにオレンジ色の十字描画（Z=0.02）。
* **独立マルチウィンドウ構成**:
  - **1つ目のウィンドウ (メインビュー)**: ロボットのバンパー先端（前方0.4m、高さ0.4m）に配置した一人称視点（FPV）カメラ (`self.cam`)。
  - **2つ目のウィンドウ (Top View Window)**: `openWindow(..., keepCamera=False, makeCamera=False)` を指定して一人称カメラの干渉を排除。コース全景の中央上空 (Z=150.0m) から真下を見下ろす独立固定カメラ (`sub_cam`) で全景マップを描画。
* **カメラ画像抽出 (`get_camera_image`)**: FPV描画バッファからスクリーンショットを取得し、OpenCV形式の BGR 配列へ変換してトピック配信。

#### 3. `robot.py` (差動二輪物理モデル)
* ROS 2 ノードとして `/cmd_vel` (`geometry_msgs/msg/Twist`) をサブスクライブ。
* 線速度 $v$ (`linear.x`) と角速度 $\omega$ (`angular.z`) に基づき、差動二輪の運動方程式に従って姿勢 $(x, y, \theta)$ を算出・更新。

---

### 2-2. `src/raphael_enterprise/raphael_enterprise/course_lap_node.py` (自律走行AIノード)

カメラ画像 (`/camera/image_raw`) のみを入力とし、画像処理＋連続トラッキング＋4点2次関数近似 (Polyfit) による高精度ビジュアルサーボ制御を行う。

#### 1. 安全装置 (Safety Guard & Emergency Brake)
* **画面最下部 ROI (Y: 85%〜100%)** のHSV色空間（Green: H 35〜85, S 40〜255, V 40〜255）を監視。
* 芝生領域のピクセル割合が **80%を超えた場合**、コースアウトと判定して `EMERGENCY` ステートへ移行。
* 即座に速度・角速度 $0$ (`Twist()`) を送信し、安全停止（二重送信防止用スパムフラグ `is_emergency_stopped` 付き）。

#### 2. 高速レスポンス・カーブ先読み判定 (Predictive Speed Control)
* **最上段スキャンライン (Y: 55%)** の黒色路面ピクセル (V $\le 50$) の重心 $C_{curve}$ を算出。
* 画面中心 ($w/2$) とのズレ $\text{offset} = |C_{curve} - (w/2)|$ を計算。
* ズレが **画面幅の 15% (`CURVE_OFFSET_THRESHOLD_RATIO = 0.15`) を超えた場合**、カーブ進入と判定してステートを `PRE_CURVE` / `TURNING` へ変更し、目標速度を減速設定 `SPEED_SLOW = 1.39 m/s` へ切替。
* ズレが収まった場合は `STRAIGHT` へ戻り、高速設定 `SPEED_FAST = 2.78 m/s` に加速。

#### 3. 連続トラッキング (Continuous Tracking) ＆ 最大ブロック抽出
足元から遠方にかけて以下の4本のスキャンラインを配し、1つ下のラインの検出重心を中心とする **探索領域 ($\pm 150\text{px}$)** 内から「最も長く連続している黒色路面ピクセルの塊（最大ブロック）」の中心を連鎖抽出。

1. **足元スキャンライン (Y: 90%)**: 画面全幅 $[0, w-1]$ から最大ブロックを抽出し、中心 `center_bottom_x` を決定。
2. **近景スキャンライン (Y: 80%)**: `[center_bottom_x - 150, center_bottom_x + 150]` のROIから最大ブロックを抽出し、中心 `center_near_x` を決定。
3. **中間スキャンライン (Y: 70%)**: `[center_near_x - 150, center_near_x + 150]` のROIから最大ブロックを抽出し、中心 `center_mid_x` を決定。
4. **遠方スキャンライン (Y: 60%)**: `[center_mid_x - 150, center_mid_x + 150]` のROIから最大ブロックを抽出し、中心 `center_far_x` を決定。

* **効果**: コース外の影や遠方のノイズ、他オブジェクトの誤検出を完全にシャットアウトし、純粋なコース路面のみを連続追従。

#### 4. 4点 Polyfit 2次曲線近似 & PID ステアリング制御
* 抽出した4点の画像座標 $(Y, X)$ から `np.polyfit(Y_array, X_array, 2)` を実行し、2次代数曲線 $X = aY^2 + bY + c$ の係数 $(a, b, c)$ を算出。
* **追従目標Y位置 (Y: 625‰)** における滑らかな理想目標点 `target_x` を逆算。
* 画面中央 ($center\_x = w/2$) との目標ズレ `error = center_x - target_x` を算出。
* **PDステアリング制御**: 
  $$\text{angular\_z} = K_p \times \text{error} + K_d \times \Delta\text{error}$$
  (パラメータ: $K_p = 0.005$, $K_d = 0.004$, 出力範囲: $[-1.0, 1.0]$)

#### 5. リアルタイムデバッグ画面・トピック出力 (`_publish_debug`)
* 画面上に各スキャンライン（55%紫、60%青、70%水色、80%黄、90%緑）、連続トラッキングROIブラケット ($\pm 150\text{px}$), 4点重心ドット, フィッティング2次曲線（赤線）, 評価目標点（赤丸）, 安全領域枠（赤枠）, 現在ステートラベルを描画。
* ROS 2 トピック `/debug/lane_view` へのパブリッシュおよび OpenCV ウィンドウ表示。

---

## 3. 主要制御パラメータ一覧 (`course_lap_node.py`)

| パラメータ名 | 設定値 | 説明 |
| :--- | :--- | :--- |
| `SCAN_Y_CURVE_RATIO` | `0.55` (55%) | カーブ先読み判定専用スキャンライン |
| `CURVE_OFFSET_THRESHOLD_RATIO` | `0.15` (15%) | カーブ減速判定の重心ズレ閾値 |
| `SCAN_Y_FAR_STEER_RATIO` | `0.60` (60%) | 軌道追従 遠方スキャンライン |
| `SCAN_Y_MID_STEER_RATIO` | `0.70` (70%) | 軌道追従 中間スキャンライン |
| `SCAN_Y_NEAR_STEER_RATIO` | `0.80` (80%) | 軌道追従 近景スキャンライン |
| `SCAN_Y_BOTTOM_STEER_RATIO` | `0.90` (90%) | 軌道追従 足元スキャンライン |
| `TRACKING_ROI_WIDTH` | `150 px` | 連続トラッキング左右探索領域幅 |
| `TRACKING_TARGET_Y_RATIO` | `0.625` (62.5%) | 2次曲線軌道上の追従評価Y基準位置 |
| `SPEED_FAST` | `2.78 m/s` (~10 km/h) | 直線走行時の最高速度 |
| `SPEED_SLOW` | `1.39 m/s` (~5 km/h) | カーブ走行時の減速目標速度 |
| `STEER_KP` / `STEER_KD` | `0.005` / `0.004` | ステアリングPD制御ゲイン |
| `EMERGENCY_GREEN_RATIO` | `0.80` (80%) | 画面下部における緊急停止芝生割合閾値 |
