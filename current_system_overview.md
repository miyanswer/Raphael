# ROS 2 ビジュアルレーシングシステム システム概要説明書

本ドキュメントは、`/home/user25/raphael_ws/src/simulation`（3Dシミュレータ環境）および `/home/user25/raphael_ws/src/raphael_enterprise/raphael_enterprise/course_lap_node.py`（ビジュアルレーシングAIノード）が、現在どのように連携し、どのような処理を行っているかを解説したものである。

---

## 1. 全体構造と処理の流れ

システム全体は **「3D環境シミュレータ（Physics & Vision）」** と **「自律走行AIノード（Image Perception & Control）」** の2つのモジュールが ROS 2 トピックを介してループ接続（閉ループ制御）される構成となっている。

```text
  ┌────────────────────────────────────────────────────────────────────────┐
  │                        3D シミュレータ (simulation)                    │
  │                                                                        │
  │  [Panda3D 3D空間]                                                       │
  │     └─ 2D/3Dコース描画 & 車両 (DifferentialDriveRobot) 物理演算            │
  │     └─ 一人称視点 (FPV) カメラ画面をリアルタイムレンダリング                │
  └───────────────────────────────────┬────────────────────────────────────┘
                                      │  Publish: /camera/image_raw
                                      │  (カメラ画像 640x480)
                                      ▼
  ┌────────────────────────────────────────────────────────────────────────┐
  │                    ビジュアルレーシング AI (course_lap_node)            │
  │                                                                        │
  │  1. 【安全判定】 画面下部で緑色(芝生)を判定 → コースアウト時緊急停止    │
  │  2. 【先読み】 画面上部で黒路面・白線を解析 → 直線/カーブ状態(State)検知  │
  │  3. 【ライントレース】 先読みROI(Y:25~65%)で路面重心(Cx)を算出           │
  │  4. 【ステアリングPID】 画面中心(center_x)との相対誤差から角速度(w)計算  │
  └───────────────────────────────────┬────────────────────────────────────┘
                                      │  Publish: /cmd_vel
                                      │  (速度 v [m/s], 角速度 w [rad/s])
                                      ▼
  ┌────────────────────────────────────────────────────────────────────────┐
  │  [simulation/main.py]                                                  │
  │     └─ /cmd_vel を受信して車両の物理状態(x, y, theta)を更新 (spin_once) │
  └────────────────────────────────────────────────────────────────────────┘
```

---

## 2. モジュール別詳細説明

### 2-1. `src/simulation` (3Dシミュレータパッケージ)

Panda3D 3Dグラフィックスエンジンおよび ROS 2 ノードを統合し、自律走行テスト環境を提供する。

*   **`main.py` (メインエントリーポイント)**
    *   ROS 2 ノード (`rclpy`) と Panda3D のメインループ (`taskMgr`) を相乗りさせて実行。
    *   毎フレーム `rclpy.spin_once()` を呼び出して AI ノードから送信される `/cmd_vel` を受診。
    *   車両物理更新 → 3D描画更新 → FPVカメラ画像キャプチャ → ROS 2トピック `/camera/image_raw` へ配信の一連のサイクルを駆動。
    *   コース周回判定（`NodeManager`）を行い、1周ラップ完了時にログを出力。

*   **`visualizer.py` (3D描画 & カメラ機能)**
    *   Panda3D を用いてアスファルトコース、白線、縁石、周囲の芝生を3D空間上にレンダリング。
    *   車両のバンパー先端（前方 $0.4\text{m}$、高さ $0.4\text{m}$）に FPV カメラを配置。
    *   `get_camera_image()` により描画バッファから BGR 画像を取得（初期化保護・ヌルガード実装済み）。

*   **`robot.py` (差動二輪ロボット物理モデル)**
    *   ROS 2 ノードであり、`/cmd_vel` をサブスクライブ。
    *   受信した線速度 `cmd_v` と角速度 `cmd_w` に基づき、運動方程式（オドメトリ計算）に従って自機位置 $(x, y, \theta)$ をリアルタイム更新。

*   **`course.py` & `config.py` (コース幾何構造 & 設定)**
    *   S字カーブ、直線、ヘアピン等で構成される特製レースコースの2D/3D幾何点を生成。

---

### 2-2. `course_lap_node.py` (ビジュアルレーシングAIノード)

カメラ画像のみを入力として受け取り、画像処理と画像ベースPID制御（ビジュアルサーボ）によってコースを自律周回するノード。

1.  **画像受信と安全装置 (Safety Guard)**
    *   `/camera/image_raw` トピックを受信。
    *   画面下部（Y: 70%〜100%）の領域で緑色（芝生）ピクセルの割合を算出。
    *   芝生割合が一定値を超えた場合、`EMERGENCY` ステートに移行して速度・角速度 $0$ (`Twist()`) を送信し、即座に安全停止（スパム送信防止フラグ `is_emergency_stopped` 付き）。

2.  **コース先読みと状態遷移 (Predictive State Control)**
    *   画面中上部（Y: 25%〜55%）の路面形状・白線ピクセル数を解析。
    *   奥の路面重心のズレから前方のカーブ進入を事前に察知し、ステートを `STRAIGHT`（高速走行: $0.6\text{m/s}$）から `PRE_CURVE` / `TURNING`（減速旋回: $0.25\text{m/s}$）へ切替。

3.  **視線先読み（Look-ahead ROI）による重心算出**
    *   自車のすぐ足元ではなく、中・長距離（Y: 25%〜65%）の路面領域から黒色路面の重心 $Cx$ を算出。
    *   車体が傾いた際の足元のブレに振り回されず、人間のように「少し先のコースの伸びる方向」を視線で注視してスムーズに旋回。

4.  **動的中心基準のステアリング PID 制御**
    *   走行ステートに応じて目標オフセット `target_x = Cx + OFFSET`（アウト寄り / イン寄り）を設定。
    *   受信画像の実際の幅 `frame.shape[1]` から画面中心 `center_x` を動的に求め、絶対基準からの相対誤差 `error = center_x - target_x` を算出。
    *   比例（Kp）＋微分（Kd）制御により制御量 `angular_z` を決定し、`/cmd_vel` トピックへパブリッシュ。
